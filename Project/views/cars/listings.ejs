<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Car Listings</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>/* General Body Styling */
        html{
            height: 100%;
        }
        
        body {
            height: 100%;
            background: linear-gradient(to bottom, #7be2ff, #044aba) !important;
        }
        
        .container-fluid {
            height: 100%;
        }
        
        .row {
            height: 100%;
        }
        
        .card {
            height: 100%;
            display: flex;
            flex-direction: column;
        }
        
        .card-body {
            flex-grow: 1;
        }
        
        /* Navbar *//* Navbar */
        .navbar {
            background-color: #ffffff;
            border-bottom: 2px solid #e0e0e0;
        }
        .navbar-brand {
            font-size: 1.8rem;
            font-weight: bold;
            color: #0d6efd;
        }
        .navbar-brand:hover {
            color: #0056b3;
        }
        .nav-link {
            color: #333;
            margin-right: 1rem;
            transition: color 0.3s ease;
        }
        .nav-link:hover {
            color: #0d6efd;
        }/* Card Titles */
        .card-title {
            font-size: 1.4rem;
            font-weight: bold;
            color: #0d6efd;
        }
        
        /* Buttons */
        .btn-success {
            font-size: 1rem;
            border-radius: 30px;
            padding: 0.6rem 1.5rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .btn-success:hover {
            background-color: #0b5ed7;
        }
        
        /* Form Styling */
        .form-label {
            font-weight: bold;
        }
        .form-control, .form-select {
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        
        /* Accordion Styling */
        .accordion-button {
            font-size: 1rem;
            font-weight: bold;
            color: #0d6efd;
            background-color: #e9f7fe;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }
        .accordion-button:not(.collapsed) {
            background-color: #0d6efd;
            color: #fff;
        }
        .accordion-body {
            background-color: #f9f9f9;
            border-radius: 10px;
        }
        
        /* ---------------------------
           New CSS Additions
           --------------------------- */
        
        /* Tabs Styling */
        .nav-tabs .nav-link {
            color: #0d6efd;
        }
        .nav-tabs .nav-link.active {
            background-color: #0d6efd;
            color: #fff;
            border-color: #0d6efd #0d6efd #fff;
        }
        
        /* Image Preview in Form */
        #imagePreview img, #editImagePreview img {
            width: 250px;
            height: 200px;
            margin-right: 10px;
            margin-bottom: 10px;
            object-fit: cover;
            border-radius: 5px;
        }
        
        /* Existing Images in Edit Modal */
        #existingImagesPreview img {
            width: 150px;
            height: 120px;
            margin-right: 10px;
            margin-bottom: 10px;
            object-fit: cover;
            border-radius: 5px;
            position: relative;
        }
        
        .remove-image-btn {
            position: absolute;
            top: 5px;
            right: 15px;
            background-color: rgba(255, 0, 0, 0.7);
            border: none;
            color: white;
            border-radius: 50%;
            width: 25px;
            height: 25px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .remove-image-btn:hover {
            background-color: rgba(255, 0, 0, 1);
        }
        
        /* Listings Styling */
        .list-group {
            display: flex;
            flex-direction: row;
            flex-wrap: wrap;
        }

        .list-item{
            background-color: #ffffff;
            height: auto;
            width: 380px;
            margin: 15px;
            border-radius: 10px;
            box-shadow: 5px 5px 15px 0px rgba(0, 0, 0, 0.5);
            justify-items: center;
            overflow: hidden;
        }
        .listing-card {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 10px;
            margin-bottom: 15px;
            background-color: #fff;
            height: auto;
        }
        
        .listing-info {
            flex: 1;
            margin-right: 10px;
        }
        
        .listing-info .card-title {
            margin-bottom: 8px;
        }
        
        .listing-info p {
            margin: 3px 0;
            font-size: 0.9rem;
        }

        .listItem-info{
            flex: 1 1 auto;
            width: 95%;
            padding-top: 20px;
        }
        
        /* Listing Images */
        .listing-images {
            width: 250px;
            height: 200px;
            position: relative;
            cursor: pointer;
            overflow: hidden; /* Ensure image fits within container */
        }
        
        .listing-images img.car-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 5px;
        }
        
        /* Remove carousel indicators and controls if any (Not needed anymore) */
        .listing-images .carousel-inner,
        .listing-images .carousel-control-prev,
        .listing-images .carousel-control-next {
            display: none;
        }
        
        /* Listing Actions */
        .listing-actions {
            margin-top: 10px;
        }
        
        .listing-actions .btn {
            font-size: 0.8rem;
            padding: 0.3rem 0.6rem;
        }
        
        /* Scrollbar Styling for Listings */
        .tab-content {
            max-height: 600px;
            overflow-y: auto;
        }
        
        /* Image Modal Styling */
        .modal-body {
            padding: 0;
        }
        
        .modal-body img {
            width: 100%;
            height: auto;
        }
        
        .carousel-control-prev-icon,
        .carousel-control-next-icon {
            background-color: rgba(0, 0, 0, 0.5);
            border-radius: 50%;
        }
        
        /* Responsive Adjustments for Listings */
        @media (max-width: 768px) {
            .listing-card {
                flex-direction: column;
                align-items: flex-start;
            }
            .listing-images {
                width: 100%;
                height: auto;
                margin-top: 10px;
            }
            .listing-images img.car-image {
                width: 100%;
                height: auto;
            }
            #imagePreview img, #editImagePreview img {
                width: 80px;
            }
            #existingImagesPreview img {
                width: 100px;
                height: 80px;
            }
            
        }
        
        /* Alert Message Styling */
        .alert {
            padding: 10px 20px;
            border-radius: 5px;
            margin-top: 15px;
            display: none;
        }
        
        .alert.show {
            display: block;
        }
        
        /*Additional*/
        .custom-img {
            width: 380px !important; /* Set the width of the image */
            height: 200px !important; /* Maintain aspect ratio */
            box-shadow: 5px 5px 15px 0px rgba(0, 0, 0, 0.5);
            
        }

        .btn a {
      color: white;
      text-decoration: none; /* Removes underline */
    }

        /* footer {
            background: white;
            height: auto;
        } */
        footer p {
            margin: 0;
            color: #ffffff;
            font-size: 2rem;
        }
        </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
            <div class="container">
                <a class="navbar-brand fw-bold text-primary" href="/main">GearShare</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <% if (user.role === 'seeker') { %>
                            <!-- Navbar for Seeker -->
                            <li class="nav-item"><a class="nav-link <% if (activePage === 'home') { %>active<% } %>" href="/main">Home</a></li>
                            <li class="nav-item"><a class="nav-link <% if (activePage === 'profile') { %>active<% } %>" href="/users/seekers/profile">Profile</a></li>
                            <li class="nav-item"><a class="nav-link <% if (activePage === 'requests') { %>active<% } %>" href="/requests/seekers/requests">Requests</a></li>
                            <li class="nav-item"><a class="nav-link" href="/">Logout</a></li>
                        <% } else if (user.role === 'provider') { %>
                            <!-- Navbar for Provider -->
                            <li class="nav-item"><a class="nav-link <% if (activePage === 'main') { %>active<% } %>" href="/main">Main</a></li>
                            <li class="nav-item"><a class="nav-link <% if (activePage === 'profile') { %>active<% } %>" href="/users/providers/profile">Profile</a></li>
                            <li class="nav-item"><a class="nav-link <% if (activePage === 'listings') { %>active<% } %>" href="/cars/listings">Listing</a></li>
                            <li class="nav-item"><a class="nav-link <% if (activePage === 'requests') { %>active<% } %>" href="/requests/providers/requests">Requests</a></li>
                            <li class="nav-item"><a class="nav-link" href="/">Logout</a></li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    
    <div class="container mt-4">
        <h1>My Car Listings</h1>
        <button type="button" class="btn btn-primary"><a href="/cars/add"><strong> Add More Vehicles</strong></a>.</button>
        <div class="listings-section">
            <% if (listings && listings.length > 0) { %>
                <div class="list-group">
                    <% listings.forEach(car => { %>
                    <div class=list-item>
                        <img src="<%= car.image %>" alt="Car Image" class="custom-img">
                        <div class="listItem-info"><h5><b><%= car.title %></b></h5>
                        <p><strong>Price per day:</strong> $<%= car.price %></p>
                        <p><strong>ID:</strong> <%= car.providerId %></p>
                        <p><strong>Type:</strong> <%= car.type %></p>
                        <div class=description><p><strong>Description:</strong> <%= car.description %></p> </div></div>  
                    </div>         
                    <% }) %>
                </div>
            <% } else { %>
                <p>No listings available.</p>
            <% } %>
        </div>
    </div>
    
    </main>
    <!-- Footer -->
    <footer class="text-center">
        <div class="container">
            <p>© 2024 GearShare. All rights reserved.</p>
        </div>
    </footer>

    <script src="/js/listings.js"></script>
</body>
</html>
